spring:
  application:
    name: EventPlanner
  datasource:
    url: jdbc:mysql://localhost:3306/event_planner
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: false
        format_sql: true